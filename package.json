{
  "name": "ball-knowledge",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm --prefix backend ci --include=dev && npm --prefix admin ci && npm --prefix admin run build && npm --prefix backend run build:api",
    "start": "npm --prefix backend start",
    "admin:dev": "npm --prefix admin run dev"
  }
}
