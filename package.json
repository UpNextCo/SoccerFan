{
  "name": "ball-knowledge",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm --prefix backend ci && npm --prefix backend run build",
    "start": "npm --prefix backend run db:migrate && npm --prefix backend start"
  }
}
