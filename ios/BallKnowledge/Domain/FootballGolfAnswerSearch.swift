import Foundation

// The new Football Golf validates typed guesses locally against the hole's shipped
// answers (pure recall — no autocomplete that would reveal answers), so the old
// server-search helper is no longer needed. Kept empty for Xcode target membership.
