import Foundation

// Football Golf is now server-driven: the daily course (holes + answers + rarity) ships
// in the daily bundle and is mapped via `FootballGolfCourse(dto:)`. This file is
// intentionally empty (kept to preserve Xcode target membership).
